# 0.8.29-dev

> 发布日期：2026-09-25

## 第七轮对齐 R1/R2：链接对象引号 + 默认对象目录

### R1 — 链接/打包对象逐文件加引号

对齐 `pfDetails::Update:579-583` 逐文件 `QuoteStringIfNeeded` + `GetTargetLinkCommands` objectSeparator 拼接：

- 链接阶段 `$link_objects` / `$link_resobjects`、静态库打包 `$link_objects` 的对象**逐项 `quoteIfNeeded` 后再拼接**；
- 修复源文件名/子目录含空格时链接与 ar 命令被 cmd 截断的问题。

### R2 — 空 `object_output` 默认 `.objs`

对齐 `CompileTargetBase::GetObjectOutput()`（compiletargetbase.cpp:203）：目标未写 `object_output` 时对象目录为 `<项目根>/.objs`（原为 `obj/`），覆盖普通对象与 PCH 对象路径。

## 已核实等价（记录）

- 构建失败时项目 post-build 不执行（扩展 `build()` 的 `ok && !cancelled` 门控，对齐 CB 错误 → bsStop）；
- deps 文件命名 `<name>.depend`（projectfile.cpp:568）与空 `deps_output` 默认 `.deps`；
- 链接对象相对路径 / .o/.a 原路径 / objectSeparator 拼接 / CommandsOnly 链接阶段只追加 post 命令（CB 怪癖）均已对齐。

## 测试

- 新增 `test-link-object-quote.js`（5 断言：空格/元字符/已带引号/拼接语义），全量回归 **38 pass**；
- 无头真实构建校验（含单文件编译/Clean 阶段）通过。

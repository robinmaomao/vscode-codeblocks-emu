# 0.8.26-dev

> 发布日期：2026-09-25

## 用户自定义错误正则（第四轮对齐 L1）

对齐 CB `Compiler::LoadSettings:699-737` 的用户正则恢复语义：

- 解析 default.conf `/compiler_sets/<id>/regex/reNNN`（description/type/regex/msg1-3/filename/line；无 description 节点跳过）；
- **按索引覆盖 XML 默认正则**（`index ≤ 现有正则数`），超出则追加；
- POSIX 正则转换提取为共享工具（`posixRegex.ts`），optionsLoader 与 CodeBlocksConfig 共用。

效果：在 Code::Blocks「高级编译器设置」里为工具链自定义过的错误/警告识别规则，现在同样在扩展中生效（错误识别、Build Log 跳转）。

### 本轮取证结论（写入对齐对照）

- 输出正则 XML 34 条全量加载、note/info 类型、msg≤3 拼接、file/line 子表达式、相对路径按 cwd 解析 —— 与 CB `ParseOutput` 一致；
- 多行错误消息仅 GNU Fortran / XML `multilinemessages` 属性启用，C/C++ 编译器恒 false —— 逐行解析等价；
- `GetCompilerTool` 扩展名匹配大小写敏感 —— 等价。

## 测试

- 新增 `test-user-regexes.js`（6 断言：覆盖/追加/无 description 跳过/未知 ID），全量回归 **36 pass**；
- 无头真实构建复验通过。

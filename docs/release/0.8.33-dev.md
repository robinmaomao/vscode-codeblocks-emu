# 0.8.33-dev

> 发布日期：2026-09-25

## 第十一轮对齐 V1：weight="0" 解析修复

对齐 CB `projectloader.cpp:1307`（`QueryIntAttribute` 直接赋值，weight 0-100 全合法、0 排最前）：

- 修复 parser 的 `Number(x) || 50` falsy 陷阱——`weight="0"` 此前被吞成默认值 50；
- 现改为显式区间判断（`0 ≤ w ≤ 100` 才接受，非法值忽略保持默认 50）；
- 序列化端本就正确（`weight != 50` 即写出），往返保留 `weight="0"`。

影响：`weight="0"` 的文件（启动汇编、预生成文件等）恢复最优先编译位置，编译顺序与 `$link_objects` 与 Code::Blocks 一致。

## 已核实等价（记录）

- `type="0"`（GUI 可执行）、关系 `="0"`（仅父级）、`compile/link/use="0"`、`prefix_auto/extension_auto="0"` 等其余 0 值属性解析均正确。

## 测试

- 新增 `test-weight-zero.js`（5 断言：解析 0 / 默认 50 / 往返保留 / 非法忽略），全量回归 **41 pass**；
- 无头真实构建校验（含单文件编译/Clean 与 distclean 阶段）通过。

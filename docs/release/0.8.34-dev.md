# 0.8.34-dev

> 发布日期：2026-09-25

## 第十二轮对齐 W1：输出文件名应用 FixPathSeparators

对齐 CB `SetupOutputFilenames`（compilercommandgenerator.cpp:654-656/741/774）的「Quote 后 FixPathSeparators」顺序：

- `setupOutputFilenames/setupStaticOutput/setupDefOutput` 在引号后应用 `fixSep`——`forceFwdSlashes=true` 的编译器下 `$exe_output/$static_output/$def_output` 转为正斜杠，与 CB 字节一致；
- gcc/clang 默认 `forceFwdSlashes=false` 行为不变（保持原生反斜杠）。

## 已核实等价（记录）

- `$link_flat_objects` / `$all_link_objects_quoted`：扩展与 CB 官方模板均不使用（仅 `$link_objects/$link_resobjects`），传参兼容保留；
- 自定义 buildCommand 宏展开走完整 `generateFromTemplate`（全宏集），对齐 CB `ExpandAllMacros`；
- 链接参数（`pf:null`、`file:''`、`deps=资源对象`、`object=linkfiles`）与 `GetTargetLinkCommands` 一致。

## 测试

- 新增 `test-force-fwd-output.js`（4 断言：exe/static/def 输出正斜杠 + 默认反斜杠不受影响），全量回归 **42 pass**；
- 无头真实构建校验（含单文件编译/Clean 与 distclean 阶段）通过。

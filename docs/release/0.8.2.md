# Code::Blocks for VS Code 0.8.2

原生移植 Code::Blocks 核心功能到 VS Code —— 直接驱动 GCC/GDB 的迷你 IDE 内核，不是 `.cbp` → `tasks.json` 的桥接。

## What's New

### 打开 `.cbp` 工程并直接编译
- 原生解析 `.cbp` / `.workspace`，完整还原项目 / 构建目标 / 文件模型
- 内置构建引擎，不依赖 `tasks.json`，增量编译（头文件 mtime 比对）
- 可视化工程属性面板，编辑后写回 `.cbp`
- 新建工程向导（Console / Static / Shared / Empty 四模板）
- 交叉编译器支持（自动读取 `default.conf`，RISC-V 开箱即用）

### 集成 Clangd
- 自动生成 `compile_commands.json` 并写入 clangd 用户配置
- 头文件智能回退上下文，解决嵌入式 SDK 的 `unknown type name`
- 自动剔除 clang 不认识的 RISC-V/GCC 专属 flag
- 未安装 clangd 时自动回退到内置符号补全

## 特性
📁 原生工程解析 · 🔨 真实构建引擎 · ⚙️ 编译器管理 · 🐞 GDB 调试 · ✨ clangd 补全 · 🔍 符号浏览器 · 🗂️ 多项目管理 · 📋 结构化构建日志

## 安装
```powershell
code --install-extension codeblocks-vscode-0.8.2.vsix --force
```

> SHA256: `E10805BB1D1F8A1E2DCF7280DACD02F7A3247886479A403AACAC9B8402DE0179`

## 快捷键
Build `Ctrl+F9` · Rebuild `Ctrl+F11` · Run `Ctrl+F10` · Debug `F8`

---
**License**: GPL v3 ｜ 详细说明见 `docs/使用说明.md`

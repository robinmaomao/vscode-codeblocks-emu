# 0.8.32-dev

> 发布日期：2026-09-25

## 第十轮对齐 U1：Clean/Rebuild 删除 deps 依赖文件

对齐 `GetTargetCleanCommands`（directcommands.cpp:955-1006）的 distclean 语义——CB 的 Clean 与 Rebuild 均以 `distclean=true` 执行（`bsTargetClean → GetCleanCommands(bt, true)`）：

- `cleanTarget` 逐文件删除对象后，追加删除 `.deps/<目录>/<名称>.depend` 依赖文件（verbose 模式 `[Clean]` 逐条列出）；
- 文件系统状态与 Code::Blocks Clean 后一致（不再残留 `.depend`）。

## 已核实等价（记录）

- `<if platform>/<else>` 条件分支：optionsLoader `evalCondition` 已实现，Switch/Option/Command/Common 加载时过滤；
- 命令模板平台变体：`options_gcc.xml` 与 CB 同构（windows 的 `cmd /c if exist` LinkStatic 与 else 的 `rm -f`），加载时按平台过滤，无混用；
- 项目 pre-build 失败中止：扩展 `build()` 失败即返回、不编译任何目标，对齐 CB 脚本 job 失败中止；
- Clean 不删 def 文件（CB 注释「.def may be user-supplied」）。

## 测试

- 无头校验 `singleFileCheck` 扩展：预置 `.deps/Debug/util.depend` 后跑 `cleanTarget`，断言 deps 与对象均被删除；
- 全量回归 **40 pass**；无头真实构建校验（含单文件编译/Clean 阶段）通过。

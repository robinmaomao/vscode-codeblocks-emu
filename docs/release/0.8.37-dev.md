# 0.8.37-dev —— 反引号全命令、单文件归属校验、deps 目录合并（第十五轮 W1-W5）

以 `Init()` 全文、`AreExternalDepsOutdated` 全文、`CompileFile`/`OnCleanFile`/`GetBuildTargetForFile`
（CB 25.03 单文件菜单真实行为）与 `DepsSearchStart` 依赖扫描目录链为基线，发现并修复 5 项。

## W1 反引号展开覆盖全部构建命令

- CB：`compilergcc.cpp:1402` 在每条非运行命令执行前统一 `cbExpandBackticks`；
  `SearchDirsFromBackticks:1286-1337` 把输出中的 `-I`/`-L` 目录汇入目标搜索目录（DepsSearchStart 使用）。
- 实现：`expandBackticks` 提为模块级函数（全局缓存对齐 `m_Backticks`）；setup*Options 三处展开时
  收集 -I/-L 目录；`renderTemplate` 末尾对最终命令整体展开（覆盖命令模板/自定义 buildCommand/脚本命令）；
  `getCompilerSearchDirs(title)` 公开关系合并目录 + 反引号派生目录。

## W2 单文件编译/清理目标归属校验

- CB：`GetBuildTargetForFile:3119-3145`——文件未归属任何目标 → 提示中止；不属于当前目标 →
  `Cannot find target for file` 中止（CompileFile:3162-3168）；OnCleanFile 静默返回。
- 实现：`compileFile`/`cleanFile` 增加 `file.buildTargets.includes(targetTitle)` 校验，
  不再用 `project.files` 兜底编译/清理未归属文件。

## W3 deps 扫描目录按选项关系合并

- CB：`m_CompilerSearchDirs` = `GetOrderedOptions(ortIncludeDirs)` 合并的项目+目标目录
  （compilercommandgenerator.cpp:803-812）；`DepsSearchStart:1208` 逐个 ReplaceMacros 后 depsAddSearchDir。
- 实现：`getIncludeDirs(target, generator)` 改用 `getCompilerSearchDirs`（关系合并 + 反引号目录），
  两处调用点（buildTarget/compileFile）宏展开逻辑不变。

## W5 单文件 Clean 只删对象文件

- CB 25.03 真实行为 `OnCleanFile:3297-3310` 只删对象；`GetCleanSingleFileCommand` 在 25.03 中无调用方（死代码）。
- 实现：`cleanFile` 移除 `.depend` 删除分支（.depend 仅全量 Clean 删除）。

## 本轮已核实等价（无需改动）

- `AreExternalDepsOutdated` 全文（静态库 libPrefix/libExtension 目录搜索、带路径库直查、extDeps/additional_output、缺失 WARNING）；
- `IsObjectOutdated` 回退语义（源缺失但存在→编译；对象缺失→编译；skip_include_deps）；
- 响应文件分割自尾部 + respFile 命名（CheckForToLongCommandLine:200-260）；
- `Init()` CommandsOnly/无编译器/平台过滤目标写空缓存存根；
- 单文件编译不执行 pre/post 脚本。

## 验证

- `tsc -p ./` 无错误；新增 `test-backticks-and-owner.js` 7 断言；全量回归 45 pass；
- headless 构建检查 EXIT 0。

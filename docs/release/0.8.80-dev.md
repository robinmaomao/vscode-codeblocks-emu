# 0.8.80-dev

> 分区标题语言跟随 VS Code 显示语言（nls）：英文默认 + 中文。

## 分区标题 nls 化

- `contributes.configuration` 7 个分区标题由硬编码中文改为 **nls 占位符**（`%configuration.*.title%`）。
- 新增 `package.nls.json`（**英文默认**）：
  `Compiler & Toolchain` / `Build & Log` / `Debug (GDB)` / `IntelliSense (clangd)` / `UI` / `Editor & Tools` / `Keybindings`
- 新增 `package.nls.zh-cn.json`（中文）：
  与 0.8.79-dev 原中文标题一致（编译器与工具链 / 构建与日志 / 调试 (GDB) / IntelliSense (clangd) / 界面 / 编辑器辅助与工具 / 快捷键）
- 行为：VS Code 显示语言为 `zh-cn` → 中文分区标题；其余语言 → 英文（默认回退）。

## 验证

- `tests/test-settings-structure.js` 追加 5 项 nls 校验（占位符格式 / 两份 nls 键完整 / 英文标题不含中文 / 中文标题含中文）→ 共 22 断言；全量 **71 测试文件 pass**。
- 重打包 `0.8.80-dev` 并安装；Reload Window 后按显示语言呈现分区标题。

# 0.8.30-dev

> 发布日期：2026-09-25

## 第八轮对齐 S1-S3：宏展开全集（MacrosManager::ReplaceMacros）

### S1 — `$(#var[.member])` 全局编译器变量

- `CodeBlocksConfig.globalVariables()` 解析 default.conf `/gcv/sets/<set>/<var>/<member>`（默认集优先合并）与旧版 `/global_uservars`（对齐 uservarmanager）；
- `$(#var)` → `base`，`$(#var.include/lib/obj/bin/cflags/lflags)` → 对应成员；值经 UnixFilename 规范化（正斜杠）。

### S2 — 内置宏集合 + 环境变量回退 + 反转义

- 补齐：`NOW/NOW_L/TODAY/TDAY/WEEKDAY`（含 `_UTC`）、`COIN/RANDOM`、`CODEBLOCKS/APP_PATH/APP-PATH/APPPATH/DATA_PATH`、`WORKSPACE_DIR/WORKSPACE_DIRECTORY`、`ACTIVE_EDITOR_*`；
- 未命中宏回退环境变量（`wxGetEnv` 语义；仍无 → 空替换，CB 同）；`$$`→`$`、`%%`→`%` 反转义；
- `TARGET_OBJECT_DIR` 空 `object_output` 回退 `.objs/`（与第七轮一致）。

### S3 — 展开应用点对齐

- 目录/选项组装点（`setup*Dirs`/`setup*Options`，对齐 806-1163）、输出文件名（`setupOutputFilenames`/`setupStaticOutput`/`setupDefOutput`，对齐 673/700/739）、最终命令（`renderTemplate`，对齐 579）、pre/post 脚本（`runScriptCommand`）、deps 扫描与外部依赖检查均接入 `replaceCbMacros`。

## 已知取舍

- `$(#var.$(FIELD))` 嵌套与 `$if(cond){...}{...}` 条件式不在本轮范围（记入对齐对照限制）。

## 测试

- 新增 `test-cb-macros.js`（13 断言：gcv base/member/cflags、日期格式、env 回退、未命中移除、反转义、嵌套、`.objs` 回退、default.conf 解析），全量回归 **39 pass**；
- 无头真实构建校验（含单文件编译/Clean 阶段）通过。

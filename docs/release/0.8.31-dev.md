# 0.8.31-dev

> 发布日期：2026-09-25

## 第九轮对齐 T1：C/C++ flag 过滤首个匹配移除

对齐 CB `GenerateCommandLine` 的 C/C++ 专属 flag 过滤（`aCflags.Index(remFlags[i])` + `RemoveAt`）：

- `filterOutFlags` 由「移除全部匹配」改为**仅移除首个匹配**——同一 flag 在项目级+目标级重复出现时字节级命令与 CB 一致；
- 保留独立 flag 边界匹配（前有空白/行首、后有空白/行尾）与带引号 flag 保护。

## 记录为限制

- 文件级 compiler 覆盖（`<Unit><Option compiler="<id>">`，CB `GetCompileFileCommand` 的 `CompilerFactory::GetCompiler(pf->compilerVar)` 整工具链切换）未实现，compilerVar 仅作 CC/CPP/WINDRES 程序选择器。

## 已核实等价（记录）

- 成功退出码：CB `exitCode >= 0 && <= statusSuccess`（compilergcc.cpp:3969/4043）；扩展同语义（Node 退出码非负）；
- weight 默认 50（对齐 CB ProjectFile 默认）；
- 链接库构造顺序（Quote → FixPathSeparators → prefix/ext → `-l`）与 CB FixupLinkLibraries 一致，无逐库宏展开（最终命令统一展开）。

## 测试

- 新增 `test-flag-filter.js`（5 断言：首个匹配/单一移除/独立边界/引号保护/cppOnly），全量回归 **40 pass**；
- 无头真实构建校验（含单文件编译/Clean 阶段）通过。

# 0.8.41-dev —— CommandsOnly 目标编译开关（第二十轮 H1）

以状态机 `bsTargetBuild`、`Init()` CommandsOnly 存根、`GetTargetLinkCommands` ttCommandsOnly 分支为基线取证。

## H1 CommandsOnly 目标文件编译行为（开关方案）

- CB 行为：`GetTargetCompileCommands`（directcommands.cpp:536-589）无目标类型过滤——CommandsOnly
  目标的 `compile=true` 文件照常编译；`Init()`（compilercommandgenerator.cpp:130-144）对 CommandsOnly
  写入空存根 → 编译命令**不带选项/include/lib 目录**；链接跳过（GetTargetLinkCommands:882-886），
  post 按 hasCommands 门控。
- 扩展默认行为（保持现状）：CommandsOnly 目标仅执行 pre/post 脚本，不编译文件。
- 新增设置 `codeblocks.build.compileCommandsOnlyTargets`：
  - `false`（默认）：保持扩展原行为；
  - `true`：对齐 CB——空存根编译文件、跳过链接、post 按 hadCommands 门控。
- 实现：`CommandGenerator.init` 对 CommandsOnly 目标写空存根（output/inc/lib/cFlags/ldAdd 全空）；
  `buildTarget`/`compileFile`/`collectCompileCommands` 三处开关门控。

## 本轮已核实等价（无需改动）

- 错误上限截断：CB `max_reported_errors`（compilergcc.cpp:3690-3704）＝ 扩展 `maxErrorsReached` 已实现；
- 队列执行模型（无 WAIT 并行、COMPILER_WAIT 屏障）＝ 按 weight 分组并行；
- `BuildStateManagement`（目标切换 RecalcVars/SwitchCompiler/工作目录）＝ `switchCompiler` + `targetMacroVars`。

## 验证

- `tsc -p ./` 无错误；新增 `tests/test-commands-only-switch.js` 5 断言；全量回归 49 pass；
- headless 构建检查 EXIT 0。

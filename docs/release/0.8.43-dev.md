# 0.8.43-dev —— Clean 确认/选中目标/工作区（第二十二轮 J1/J2/J3）

以 `OnClean`/`OnCleanAll`、`CleanWorkspace`、`GetCleanCommands` 调用链为基线取证。

## J1 Clean 确认对话框

- CB：`OnClean:3385-3397`——清理前 `AnnoyingDialog` 确认，NO 则中止；`OnCleanAll` 同。
- 实现：`confirmClean(scope)` 模态确认（「…将删除所有相关对象文件…是否继续清理？」），取消即中止。

## J2 Clean 目标范围 = 选中目标

- CB：`OnClean → Clean(target=nullptr) → DoBuild("")`，`realTarget = GetTargetString()` = 当前选中目标；
  虚拟目标经 `ExpandTargets` 展开组。
- 原状：清理项目全部目标。
- 实现：`clean()` 改为清理选中目标（未设则回退首个平台支持目标）；`cleanTargets()` 虚拟目标展开组
  逐个 `cleanTarget`。

## J3 新增 Clean Workspace 命令

- CB：`OnCleanAll → CleanWorkspace()`——全部已打开工程各自的选中目标 + 依赖拓扑排序。
- 实现：`codeblocks.cleanWorkspace` 命令（项目树 view/title 入口，navigation@10）+ `cleanWorkspace()`
  函数（确认 → 停止调试 → 保存 → 拓扑遍历清理）。

## 本轮已核实等价（无需改动）

- `AddCommandsToArray` 多行命令拆分 = `runCommand` 拆多行逐条执行；
- `GetCleanCommands(target=null)` 的 includeInTargetAll 过滤为遗留路径——菜单流经 PreprocessJob 按选中目标解析；
- Clean 状态机（无 pre/post 脚本、Rebuild 双遍）此前已核实。

## 验证

- `tsc -p ./` 无错误；全量回归 50 pass；headless 构建检查 EXIT 0。

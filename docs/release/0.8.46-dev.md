# 0.8.46-dev —— 全量差异扫描 + 宏系统补全（第二十五轮 M1/M2/M3/A1）

本轮为全量差异/异常扫描轮，除常规取证外覆盖 `CompilerFactory::GetCompiler`、
`macrosmanager` 全宏表、`AddCommandsToArray`、`DoClean`、`InitBuildState` 与扩展自身
代码异常标记扫描，发现并修复 4 项。

## M1 编译器 ID 解析回退

- CB：`CompilerFactory::GetCompiler`（compilerfactory.cpp:42-58）——大小写不敏感匹配；
  未命中按去掉 `-` 的旧 ID 格式二次匹配。
- 实现：`resolveTargetCompiler` 候选序列（原 ID → 小写 → 去 `-`，去重）；XML 文件
  大小写不敏感匹配；用户编译器沿用 codeblocksConfig 的 `_`↔`-` 变体。

## M2 静态内置宏补全

- CB `ClearProjectKeys`（macrosmanager.cpp:128-163）与 `RecalcVars:396`：
  `AMP/PLATFORM/CMD_CP/CMD_RM/CMD_MV/CMD_MKDIR/CMD_RMDIR/CMD_NULL/PLUGINS/LANGUAGE/ENCODING/`、
  `DATA-PATH/DATAPATH`、`WORKSPACE_FILE/WORKSPACE_FILENAME/WORKSPACE_FILE_NAME/WORKSPACEFILE/`、
  `WORKSPACEFILENAME/WORKSPACE_NAME`、`TARGET_COMPILER_DIR`。
- 实现：`cbBuiltinVars` 与 `buildMacroVars` 双实现同步补齐（compilerDir 可选参数贯通
  commandGenerator.cbVars / buildEngine.targetMacroVars）。

## M3 函数式宏补全

- CB：`$TO_ABSOLUTE_PATH{}`（相对当前工作目录转绝对）、`$TO_83_PATH{}`（绝对化后 8.3 短路径）、
  `$REMOVE_QUOTES{}`（内部宏展开 + 首尾引号剥离），且**先于变量替换**执行（macrosmanager.cpp:610-667）。
- 实现：`replaceCbMacros` 在变量循环前处理三个函数式宏（含 REMOVE_QUOTES 内部宏递归）；
  新增 `basePath` 可选参数（commandGenerator/renderTemplate/scriptRunner 传入）。

## A1 代码清理

- 移除 `commandGenerator.ts` 的冗余 `(this.project as any).resourceCompilerOptions`。

## 验证

- `tsc -p ./` 无错误；新增 `tests/test-builtin-macros.js` 13 断言；全量回归 52 pass；
- headless 构建检查 EXIT 0。

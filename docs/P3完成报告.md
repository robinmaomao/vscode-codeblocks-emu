# 执行进度报告：P3（辅助工具）

> 全部通过 `npx tsc -p ./` 编译（0 错误），关键逻辑已实测验证。

## P3 新增实现

| 模块 | 文件 | 对应 Code::Blocks | 说明 |
|------|------|------------------|------|
| TODO 扫描器 | `src/tools/todoScanner.ts` | `todo_parser.cpp ParseBufferForTODOs` | 识别 TODO/FIXME/NOTE/HACK/XXX，解析优先级 `(n)`、用户 `user:`、日期 `YYYY-MM-DD` |
| 代码统计 | `src/tools/codeStats.ts` | 代码统计插件 | 统计代码/注释/空行/总行数，支持聚合 |
| AStyle 格式化 | `src/tools/astyle.ts` | `astyleplugin.cpp` | 调用外部 astyle，回退 VS Code 内置格式化 |
| 项目树视图 | `src/ui/projectTreeProvider.ts` | 项目树（虚拟文件夹） | TreeDataProvider 展示目标/文件/目录层级 |

## 实测验证结果

### TODO 扫描
```
// TODO: fix this bug                          → TODO, line 1
// FIXME (2) alice: 2024-01-15 memory leak     → FIXME, priority=2, user=alice, date=2024-01-15
/* NOTE: block comment */                      → NOTE, text="block comment"（已清理 */）
```

### 代码统计
```
7 总行 = 3 代码 + 3 注释 + 1 空行
```

## 完整功能清单（全部完成）

| 阶段 | 功能 | 状态 |
|------|------|------|
| P0 | .cbp/.workspace 解析、命令行生成（宏展开）、构建引擎、错误解析 | ✅ |
| P0 收尾 | options XML 完整解析、增量编译、wxRegEx→JS 兼容 | ✅ |
| P1 | 编译器自动探测（GCC/MSVC/Clang）、编译选项 UI | ✅ |
| P2 | 自研 DAP 调试器（GDB MI） | ✅ |
| P3 | 代码统计、TODO、AStyle、项目树 | ✅ |

## 完整源码结构（17 个 TS 文件）

```
src/
├── extension.ts              扩展入口
├── model/
│   ├── types.ts              数据模型（TargetType/Project/BuildTarget 等）
│   └── parser.ts             .cbp/.workspace 解析
├── compiler/
│   ├── compiler.ts           编译器模型 + 命令模板
│   ├── optionsLoader.ts      options_*.xml 完整解析（extends/平台条件）
│   ├── commandGenerator.ts   宏展开 + 命令行生成
│   └── detector.ts           编译器自动探测
├── build/
│   ├── buildEngine.ts        构建引擎（派生进程 + 增量编译）
│   └── outputParser.ts       错误/警告解析 → Diagnostics
├── debug/
│   ├── gdbMiSession.ts       GDB MI 会话
│   └── gdbDebugAdapter.ts    DAP 调试适配器
├── tools/
│   ├── todoScanner.ts        TODO 扫描
│   ├── codeStats.ts          代码统计
│   └── astyle.ts             AStyle 格式化
└── ui/
    ├── projectTreeProvider.ts 项目树视图
    └── compilerOptionsPanel.ts 编译选项 Webview
```

## 剩余可选项（非核心，2026-09-24 更新）

| 项 | 状态 |
|----|------|
| pre/post build 脚本（`.bat` / 命令） | ✅ 已实现（scriptRunner） |
| Squirrel 脚本引擎 | ❌ 未实现（边缘场景） |
| DAP 变量子节点展开 | ✅ 已实现（scopes + variables） |
| DAP 监视窗口（Watch） | ✅ 可用（evaluate request） |
| DAP 条件断点 | ❌ 未实现（supportsConditionalBreakpoints: false） |
| DAP 函数断点 | ❌ 未实现（supportsFunctionBreakpoints: false） |
| 打包 `.vsix` | ✅ 已实现（@vscode/vsce） |
| Code::Blocks 快捷键对齐 | ✅ 已实现（F9 / Ctrl+F9 等） |

## 许可证
- TypeScript 代码为独立重写；`resources/compilers/*.xml` 源自 GPL v3 的 compilergcc 资源，整体发布需 GPL v3 开源。

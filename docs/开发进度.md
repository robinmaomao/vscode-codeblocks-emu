# CodeBlocks Power by VS Code —— 开发进度快照

> 本文件用于**恢复开发进度**。改动大、需要中断时，更新此文件即可保证后续能快速续上。

## 一、总体进度

| 阶段 | 状态 | 说明 |
|------|------|------|
| P0 工程属性编辑 | ✅ 完成 | 8 个 tab 的「工程属性」WebView 面板，可视化编辑并写回 .cbp |
| P1 新建工程向导 | ✅ 完成 | 4 模板生成 .cbp + 骨架源文件 |
| P1 构建目标可视化切换 | ✅ 完成 | 已重构对齐 Code::Blocks：文件只出现一次，目标切换用状态栏/右键（工程树不含目标层级） |
| P2 工程间依赖 (.workspace) | ✅ 完成 | 解析 `<Depends>` 依赖 + DFS 拓扑排序自动决定编译顺序 |
| P3 文件级自定义命令 UI | ✅ 完成 | 文件属性面板编辑 + 工程树文件节点右键快捷入口 |
| P3 性能优化 | ✅ 完成 | 三大缓存（include 依赖 / 工程树懒加载 / XML 解析）+ LRU + 盘符归一化 |

## 二、P0 工程属性编辑（已完成）

面板 `src/ui/projectPropertiesPanel.ts`，命令 `codeblocks.projectProperties`，**8 个 tab**（左侧竖排导航 + 顶部项目标题栏）：

1. **构建目标**：增删改（标题/类型/输出文件/对象目录/编译器 ID），add/copy/remove
2. **虚拟目标**：alias + 物理目标多选（All = Debug;Release）
3. **文件**：compilerVar/compile/link/weight(0-100)/virtualFolder/自定义构建命令/目标归属多选
4. **构建选项**：编译选项/链接选项/链接库 + 目标级 **OptionsRelation 关系**（5 个下拉：仅父级/仅目标/前置/追加）
5. **搜索目录**：include/lib/resource 目录（项目级+目标级）
6. **构建脚本**：`<Script file>` + **pre/post build 命令**（`<ExtraCommands><Add before/after>`）
7. **项目设置**：标题/默认编译器/虚拟文件夹
8. **备注**：notes + show_notes（CDATA）

核心文件：
- `src/model/types.ts`：数据模型（新增 buildScripts/notes/showNotesOnLoad/weight/virtualFolder 字段）
- `src/model/parser.ts`：.cbp 解析（对齐 projectloader.cpp）
- `src/model/projectWriter.ts`：.cbp 序列化（serializeProject，对齐 ExportTargetAsProject）
- `src/ui/projectPropertiesPanel.ts`：WebView 面板
- `src/extension.ts`：`saveProjectProperties(project, targets, files, options, searchDirs, projectSettings, buildScripts, notes, virtualTargets)` 写回 + 重解析刷新

## 三、P1 新建工程向导（已完成）

- `src/project/newProject.ts`：`PROJECT_TEMPLATES`（Console/Static/Shared/Empty 四模板）+ `createProjectFromTemplate(name, basePath, tpl)`
- 命令 `codeblocks.newProject`：QuickPick 模板 → InputBox 名称 → showOpenDialog 目录 → mkdir + 写 main.c + 写 .cbp → openProject
- 入口：项目树标题栏 `$(new-file)`、Menu 视图 File → New Project…

## 四、P1 构建目标可视化切换（已完成）

- `src/ui/projectTreeProvider.ts`：
  - TreeNode 新增 `kind: 'target'` + `targetTitle` 字段
  - 层级：**项目 → 构建目标 → 该目标的文件**（`buildTargetNodes` + `buildFileNodes(targetTitle)` 过滤）
  - `selectedTargets` Map + `setSelectedTargets()`；`getTreeItem` 动态高亮（活动目标绿点 + `▶ 活动`）
- 命令 `codeblocks.setActiveTarget`（目标右键「设为活动目标」，`when viewItem =~ /^target/`）
- `setSelectedTarget()` 内部同步 provider；openProject 后同步
- package.json view/title group 已重编号 navigation@1..10

## 五、P2 / P3（已完成）

### P2 工程间依赖（.workspace）✅
- `WorkspaceParser` 解析 `<Depends>` 依赖 → DFS 拓扑排序（依赖先编译、环检测）；打开 `.workspace` 自动打开全部项目；reload 后恢复全部项目 + 依赖（`restorePersistedProjects`）。

### P3 文件级自定义命令 UI ✅
- 工程树文件节点右键「Edit Custom Build Command...」编辑 `<Option buildCommand>` 写回 `.cbp`（文件属性面板「文件」tab 也支持多行编辑）。

### P3 性能优化 ✅
- 大工程三项缓存：include 依赖缓存（buildEngine）、工程树懒加载（projectTreeProvider）、XML 解析缓存（parser）；缓存 key 盘符归一化 + LRU 容量上限（`src/tools/lru.ts`）。

## 六、构建 / 打包 / 安装命令

```powershell
# 编译（Windows 用 npx.cmd）
npx.cmd tsc -p ./

# 打包 VSIX
npx.cmd vsce package --allow-missing-repository

# 安装
code --install-extension "codeblocks-vscode-0.8.2.vsix" --force
```

## 七、测试脚本（都在工作区根，已加入 .vscodeignore 不打进 VSIX）

| 脚本 | 用途 |
|------|------|
| test-writer.js | parse→serialize→reparse 往返一致 |
| test-target-edit.js | 目标增删改 + 文件归属同步 |
| test-file-edit.js | 文件编译选项/归属/自定义命令 |
| test-options-edit.js | 编译/链接选项 + 库 |
| test-dirs-settings-edit.js | 搜索目录 + 项目设置 |
| test-scripts-notes-edit.js | 构建脚本/备注/weight/virtualFolder |
| test-virtualtarget-edit.js | 虚拟目标 |
| test-relation-extracommands-edit.js | OptionsRelation + ExtraCommands |
| test-newproject.js | 4 个工程模板生成 |
| test-html-syntax.js | 检查 WebView `<script>` 语法（改面板后必跑） |
| test-native-sep.js / test-drive-case.js | `$file` 斜杠 / 盘符归一化 |
| test-workspace-deps.js | .workspace 依赖解析 + 拓扑排序 |
| test-weight-sort.js | weight + 字节序排序 |
| test-ar-quote.js / test-resp-file.js / test-link-static.js / test-static-prefix.js | 静态库 / 响应文件 / 链接对齐 |
| test-vfolder*.js / test-categorize-sim.js / test-tree-lazy*.js | 虚拟文件夹 / 分组 / 懒加载树 |
| test-parse-cache.js / test-deps-cache.js / test-lru.js | 解析 / include 依赖 / LRU 缓存 |
| test-makecommands.js / test-implib-roundtrip.js / test-roundtrip-notes.js / test-unit-options.js | 序列化往返 |

## 八、关键踩坑记录（务必遵守）

1. **TS 模板字符串内嵌 JS 的转义**：在 `buildHtml()` 的反引号模板里写前端 JS 时，JS 里的 `'\n'` 必须写成 `'\\n'`（双重转义），否则 TS 会把它解释成真实换行导致前端 script 语法错误、面板空白。改 WebView 后必跑 `test-html-syntax.js`。
2. **`<{~None~}>`**：Code::Blocks 用该特殊值表示「文件不归属任何目标」，parser 已特判（buildTargets=[]）。
3. **`useConsoleRunner` 默认 true**（Code::Blocks 读取默认 true，非 false）。
4. **ResDirs 关系键**：`projectResourceIncludeDirsRelation`（不是 projectResIncludeDirsRelation）。
5. **`test-project/hello-cb.cbp`** 曾被面板保存测试写回为 type=0/3，已 `git checkout` 恢复（type=1）。测试脚本都写 tmp 文件，不写原文件。
6. **Code::Blocks 25.03 已无 priority 字段**（projectfile.h 只有 weight）。
7. **日志输出超 20KB 会写文件**，用 read_file 读取。

## 九、当前状态（2026-09-26）

> **第四十九轮 调试器能力对齐（0.8.76-dev，零影响第一批 A 组 11 项）**：
> - **DAP 能力扩展**（`src/debug/gdbDebugAdapter.ts`）：`disassemble`（原生反汇编视图，mode 1 混合源码 + 按 instructionOffset 自适应窗口）/ `readMemory`+`writeMemory`（原生 Memory 视图；指针变量回填 `memoryReference`）/ `dataBreakpoints`（`dataBreakpointInfo` + watch/rwatch/awatch）/ `supportsSteppingGranularity`（指令级步进）/ `supportsHitConditionalBreakpoints`（`-break-after`）+ `supportsLogPoints` / `gotoTargets`+`goto`（运行到光标，`-exec-until`）/ `setExceptionBreakpoints`（throw/catch catchpoint，`-break-list` 回收）/ `attach`（`-target-attach`，附加后不 `-exec-run`）；Debug Console 以 `-` 前缀透传 MI（Send user command）；调试器设置注入（initCommands / printPretty / printElements / disassemblyFlavor / charset）；
> - **新模块**：`src/debug/miParse.ts`（纯解析：反汇编/寄存器/内存/进程/catchpoint）、`src/debug/debugRegistry.ts`（适配器↔UI 注册表）、`src/ui/registersTreeProvider.ts`（寄存器视图，调试容器 `views.debug`，停驻自动刷新）；
> - **新命令**：`Refresh Registers` / `Send GDB Command…` / `Set Next Statement` / `Attach to Process…`（tasklist/ps 进程选择）；**新设置** 5 项（`codeblocks.debug.*`）；
> - 测试：新增 `tests/test-debug-protocol.js`（20 断言：反汇编两种模式/窗口语义/寄存器/内存往返/catchpoint/进程列表/指针判定）→ 全量 **61 pass**；headless EXIT 0；文档同步 0.8.76-dev。
> - **修复**：运行/调试的可执行文件存在性检查未做 Windows `.exe` 回退（如 test-project 的 `output="bin/Debug/hello"` 实际产出 `hello.exe`）→ 新增共用解析 `src/build/outputPath.ts`（链接时间戳检查同源）+ `tests/test-output-path.js`（8 断言），错误提示附实际查找路径。
> - **P1 调试缺陷修复（D1–D11）**：`terminate` 回收 GDB（停止后不再残留进程）/ 程序退出发 `exited`+`terminated` 结束会话 / **栈帧改逐 tuple 解析（修复 file/line 丢失，fullname 优先）** / **日志断点真正生效**（`{expr}` 求值输出后自动继续）/ 停止原因按 DAP 枚举映射（watchpoint→data breakpoint、signal→pause/exception）/ 调试工作目录对齐 `runWorkingDir` / 库目标 F8 走宿主程序 / **本地变量按帧作用域**（`-stack-select-frame`）/ 新增 `codeblocks.debug.gdbPath` + 定位优先级与尝试路径提示 / `initialized` 移至会话就绪后 + 启动失败清理 / 求值失败可见；测试 `test-debug-protocol.js` 扩至 29 断言 + 新增 `tests/test-debug-locate.js`（7 断言）。
> - **修复 2（GDB 启动方式）**：程序不再作为命令行位置参数传给 GDB —— MinGW GDB（已实证）会把含空格路径按空格二次切分（`E:\Work_Share\VSCode` + `Workstation\...\hello.exe` 两段 → `No executable specified`）；改为会话建立后 MI 注入 `-file-exec-and-symbols` / `-exec-arguments`（attach 同源，符号文件可选加载）。
> - **修复 3（行断点从未插入）**：旧 `-break-insert -f <文件> -l <行号>` 为非法 MI（实测 `Garbage following <location>`）→ 改为位置串 `<文件>:<行号>`（实测可命中）；条件断点改为插入后 `-break-condition`（`-c` 即时求值报 `No symbol`）；`pending="..."` 识别为未验证并回传提示；函数断点条件同源处理。测试 `test-debug-protocol.js` 增至 32 断言。
> - **修复 4（F8 语义）**：F8 键位去 `!inDebugMode` 门控（`editorTextFocus`），命令内改为 **Start / Continue**：有活动调试会话 → `workbench.action.debug.continue`，否则启动调试——对齐 CB debugger_menu 的 Start / Continue 单项（旧行为在调试中按 F8 回落到 VS Code「下一错误」）。
> - **修复 5（协议违规 + 跟踪开关）**：停顿时不再伪造 `threads` 响应（request_seq=0，属 DAP 协议违规，可能被新版 VS Code 提前终止会话）——改为依赖客户端 stopped 后的正常请求；新增设置 **`codeblocks.debug.trace`**：把 DAP 请求/响应/事件写入 Code::Blocks 输出通道（排查「会话异常结束」的定位工具）。用 mock-vscode 驱动真实适配器+GDB 全流程验证：停顿后静置 3 秒无自发 terminated，continue 后才 exited+terminated（预期）。
> - **修复 6（停住后退出 = GDB 崩溃，实锤）**：跟踪日志显示停顿后发出 `exited{exitCode=3221225477}`（0xC0000005）——逐项复现确认**部分 MinGW GDB 8.1 构建读取任何寄存器值（`-data-list-register-values x/N`、CLI `info registers`）都会让 GDB 进程崩溃**，而停顿时 Registers 视图自动刷新恰好触发它；修复：寄存器读取改为设置 **`codeblocks.debug.registers`（默认关）** + GDB 异常退出时向 Debug Console 输出可读诊断（含 0xC0000005 提示）。
> - **修复 7（程序输出不可见）**：实测原始 MI 流发现——MinGW GDB 下**被测程序 stdout 以“无 MI 前缀”的裸文本形式直接写入管道**（既不包 `@"…"` 也不包 `~"…"`，形如 `hello-cb: 2 + 3 = 5`），此前被解析器静默丢弃；修复：`isUnframedLine` 识别裸文本行并转发为程序输出（`@`/`~`/`&`/结果与异步记录/`(gdb)` 提示符均不误判）。端到端（真实 GDB + 模拟 VS Code）回归：断点停住 → continue → `stdout| hello-cb: 2 + 3 = 5` → `exited exitCode=0`。
> - **修复 8（Step Out 使会话退出 = GDB 又崩溃）**：日志筛查（4 会话全量 DAP 请求）+ 原始 MI 取证确认：**在 `add()` 内发 `-exec-finish` 会让本机 GDB 8.1 崩溃（仅回 `^running` 后退出码 3221225477，同 0xC0000005）**；在 `main`（最外层）则报 `"finish" not meaningful in the outermost frame`。修复：Step Out 改为 **`-stack-list-frames` 取 frame 1（调用者）返回地址 → `-break-insert -t *addr` 一次性断点 + `-exec-continue`**（已实证不触发崩溃且准确停在调用处）；命中后上报 `reason=step`；最外层时给控制台可读提示而不结束会话。端到端回归：`add()` 内 Step Out → `main@main.c:7` 存活 ✓；`main` 内 → 提示 + 会话保持 ✓。另查明：**VS Code 的 Run to Cursor 是“临时断点 + 继续”流程**（非 `gotoTargets`），实测停点准确且会话存活。
> - **修复 9（条件断点命中即 GDB 崩溃）**：原始 MI 取证：**只要“带条件的断点”被命中，本机 GDB 8.1 必崩溃**（`result == 5`、`1 == 1`、`0 == 1` 全崩；无条件命中正常、`-break-after` 正常、`-data-evaluate-expression` 手工求值正常——崩溃在 GDB 自身的断点条件求值机制）。修复：条件断点改为**客户端求值**（GDB 只挂无条件断点，命中后由前端 `-data-evaluate-expression` 判断，不成立自动继续；同日志断点机制），文件断点与函数断点一致；命中次数 `-break-after` 修正为忽略 N-1 次（对齐 DAP「第 N 次命中时停」语义）。
> - **修复 10（多工具链覆盖加固）**：`applyDebugSettings` 开启 `-gdb-set mi-async on`（让 Pause 在支持的 GDB 上可用，老 GDB 忽略失败）；栈帧补充 `instructionPointerReference`（VS Code「反汇编视图」定位当前指令必需）；条件求值结果判定 `truthyMiValue`（1/0/true/false/0x…）。
> - **兼容性联调（第二套工具链实测）**：MinGW.org **GCC 6.3（32 位产物）** + **GDB 7.6.1**：launch/断点/stepIn/stepOut（返回地址方案在老 GDB 同样工作）/next/本地变量/客户端条件求值/正常退出 **9/9 全过**；MinGW-w64（64 位）+ GDB 8.1 全功能 **31/31 过**（含反汇编/内存读写/数据断点/hitCondition/goto/异常断点/terminate）。**已适配的限制**：Pause 依赖 GDB 平台中断支持（此 GDB 在无控制台环境不处理运行中命令，三种中断方式实测均无效——平台限制，非扩展缺陷）；MSVC/PDB 不在 GDB 范围，文档已注明。
>
> **第四十八轮 设置界面面板入口（0.8.74-dev）**：
> - 扩展设置界面（`Ctrl+,` → Extensions → Code::Blocks）中两项快捷键设置 `codeblocks.keybindings.cbStyle` / `codeblocks.keybindings.overrides` 的 `markdownDescription` 增加 **`command:` 链接**（打开可视化面板 / 检查冲突）——VS Code 设置描述 markdown 由 `settingsTree` 以 `openerService.open(content, { allowCommands: true })` 处理，点击即执行命令（与 PowerShell 等扩展同机制）；
> - 新增 `tests/test-settings-command-links.js`（递归扫描 configuration 全部 markdown 字段：链接目标必须在 `contributes.commands` 或内置 `workbench.*` 白名单内、URI 无空白/中文/括号、两项快捷键设置必须保留面板入口）→ 全量 **58 pass**；headless EXIT 0；文档同步 0.8.74-dev；
>
> **第四十七轮 快捷键可视化设置面板（0.8.73-dev，方案 A + D5）**：
> - **新面板** `src/ui/keybindingPanel.ts`（Webview，仿工程属性面板模式）：一屏 30 项分组（可折叠/搜索）列表；行内显示当前键 / 默认键 / 生效状态 / 冲突与重复注解；操作：**录入（按键捕获）** / 手动输入 / 解绑 / 默认；
> - 顶部操作：应用 / 全部重置（模态确认）/ 检查冲突 / 打开 keybindings.json / **导出方案 / 导入方案（D5）**；修改立即写设置并应用（复用托管写入：仅托管条目/首次备份/回读校验回滚）；
> - **纯数据**（keybindingConfig 新增）：`buildKeybindingRows`（行模型：状态/ok/冲突与跨项重复注解）、`buildExportPayload` / `parseImportPayload`（方案负载，兼容包装与直接映射）；
> - 新命令：`Keybinding Settings`（Menu → Settings → Keybindings… 入口）、`Export / Import Keybinding Scheme`；`Reset Keybindings` 重构为命令/面板共用（`performKeybindingsReset`）；`applyKeybindings` 返回结果对象供面板反馈；
> - 测试：新增 `tests/test-keybinding-panel-model.js`（16 断言）+ `tests/test-keybinding-panel-html.js`（WebView 脚本语法）→ 全量 **57 pass**；headless EXIT 0；文档同步 0.8.73-dev（使用说明 §12.5 改写）；
> - 已知边界：被 VS Code/系统优先占用的组合（Ctrl+W 等）无法在 Webview 捕获，提供手动输入兑底。
>
> **第四十六轮 快捷键可配置化（0.8.72-dev，方案 A + D7）**：
> - **纯逻辑模块** `src/tools/keybindingConfig.ts`：托管命令表（30 项 = 15 内置 + 3 外部别名 + 12 CB 保真组）、chord 校验（`validateChord`，支持两段组合键）、`overrides` 解析（未知/非法项单独报告）、期望条目生成（自定义键 → 正向条目 + 对默认键的 `-command` 移除规则；空 = 解绑）、**条目级文本手术**（注释保留 / 尾部逗号 / 空文件 / 纯注释文件 / 幂等）、D7 差异（`diffManaged`）；
> - **设置** `codeblocks.keybindings.overrides`（object）= 唯一数据源；修改后**自动应用**（去抖 800ms）写入用户 `keybindings.json`；
> - **新命令**：`Apply Keybindings`（手动应用）、`Configure Keybindings…`（向导：选命令 → 输入新键带校验；Menu → Settings → Keybindings… 入口）、`Reset Keybindings`（清托管条目 + 清空覆盖设置）；
> - **安全边界**：只增删托管条目、其它条目与注释原样保留；首次写入备份 `keybindings.json.codeblocks-backup`；写入后回读校验失败自动回滚；
> - **D7**：`Check Keybinding Conflicts` 增加「托管覆盖 vs 当前生效」清单（已生效 / 未写入 / 未知键名 / 无效值）；
> - 新增 `tests/test-keybinding-config.js`（27 断言：表↔package.json 双向一致、chord 校验、overrides 解析、期望条目、文本手术 6 组 fixture、D7 差异、规模）→ 全量 **55 pass**；headless EXIT 0；文档同步 0.8.72-dev（使用说明 §12.5）；
> - 教训：托管条目差异比较必须同时纳入“移除规则”条目，且一致条目应保留而非删除后不重加——否则 apply 不幂等（回归测试捕获）。
>
> **第四十五轮 快捷键补全与冲突处理（0.8.71-dev）**：
> - **默认新增安全键**（全部避开 VS Code 默认）：`Alt+F1/Alt+F2`（错误导航，CB 键位）、`Alt+G`（Goto file）、`Shift+F2`（Project 视图聚焦）、`Ctrl+Shift+R`（Replace in files，`!inSearchEditor`）、`Ctrl+Shift+Up/Down`（Project tree 移动，`focusedView` 作用域）、`Ctrl+E`（选择下一个匹配）；菜单结构同步标注（回归测试强制一致性）；
> - **CB 保真模式**：新设置 `codeblocks.keybindings.cbStyle`（默认 false，when 门控 `config.codeblocks.keybindings.cbStyle`）——12 项：`F5` 断点 / `Shift+F8` 停止调试 / `F2` Build Log / `Ctrl+R` 替换 / `Ctrl+Shift+B` 括号 / `Ctrl+Shift+C` 注释 / `Ctrl+Shift+S` 全部保存 / `Ctrl+Q` 退出 / `F12` 折叠 / `F7`·`Shift+F7`·`Ctrl+F7` 步进；开启时一次性提醒并可直达冲突清单；
> - **冲突检测**：新命令 `codeblocks.keybindings.check`（三源扫描：扩展自身 / 用户 keybindings.json / 其他已安装扩展 package.json + 内置 VS Code 默认冲突表；QuickPick 清单 + 输出通道报告 + 打开键盘快捷方式/JSON/复制报告）；启动时仅对用户级真实撞车静默提示（输出通道 + 一次性状态栏项，不弹窗，globalState 去重）；
> - **纯逻辑模块** `src/tools/keybindingConflicts.ts`（`parseJsonc` / `normalizeKey` / `collectConflicts` / `VSCODE_DEFAULT_CONFLICTS`，无 vscode 依赖，供运行时与测试共用）；
> - 新增 `tests/test-keybindings.js`（15 断言组：唯一性/门控集合/默认清单/命令有效性/when 合法性/冲突分级/JSONC 解析）→ 全量 **54 pass**；headless EXIT 0；文档同步 0.8.71-dev（使用说明新增 §12 快捷键章节：已对齐 / 安全新增 / CB 保真 / 冲突检测）；
> - 教训：JSDoc 注释内出现 `*/` 序列（如 `extensions/*/package.json`）会提前终止块注释导致 TS1127——注释里写路径用 `<扩展目录>` 代替 `*`。
>
> **第四十四轮 Menu 对齐（0.8.70-dev）**：
> - **结构数据层**：新增 `src/ui/menuStructure.ts`（纯数据、无 vscode 依赖）——9 个顶级菜单、92 个菜单项（子菜单/分隔线/快捷键/needsProject 标记）；`statusBarMenu.ts` 收敛为渲染层（两级/三级 QuickPick）；**悬停菜单与结构共用同一数据源**（label 引用结构 + 回归测试校验）；
> - **渲染能力**：第二级支持**子菜单下钻**（Project tree / Folding / Case / Line / Errors）、**分隔线**、**快捷键标注**、未打开工程时相关项标注**（无工程）**；第一级 Recent Projects 末尾新增 **Clear Recent Projects**；
> - **Build 菜单补全**（对齐 compiler_menu.xrc 全项）：Compile Current File（新命令 `codeblocks.compileCurrentFile`，Ctrl+Shift+F9）/ Build·Rebuild·Clean Workspace / Abort / Errors 子菜单（Previous·Next·**Clear All**）/ Select Target（从 Project 移入）/ Export compile_commands.json；顺序按 CB 重排（Build → Compile Current File → Run → Build and Run → Rebuild → Clean）；
> - **其余菜单**：Search（Find/Find in Files/Find Next·Previous/Replace/Replace in Files/Goto Line/Goto File）、Edit（Toggle·Block Comment/Folding/Case/Line 子菜单/Select Next Occurrence/Goto Matching Brace）、View（Symbols/Analysis/Problems/Terminal/Full Screen/Reset View Layout）、File（Save As/Close All Files/Quit）、Debug（Break/Stop/Step×3/Toggle Breakpoint/Run and Debug 视图——映射 VS Code 原生调试命令）、Settings（`workbench.action.openSettings` 过滤串 + 全局变量只读 + 清空反引号缓存）；
> - **Project 菜单**：Add Files…（**无树节点时回退活动工程**，同 Move Up/Down、Remove Project）、Project tree 子菜单（Move、**Activate prior/next = Alt+F5/F6**、Categorize 开关）、Notes…（属性面板备注 tab 直达）、**Set Programs' Arguments…**（属性面板构建目标页新增「运行」组 → 写回 `<Option parameters>`）、Properties；
> - **命令修复**：`codeblocks.addFile/moveProjectUp/moveProjectDown/removeProject` 从 Menu/命令面板调用不再静默无效（活动工程回退）；View → Project 聚焦先打开侧栏容器（避免容器未打开时不生效）；
> - **测试**：新增 `tests/test-menu-structure.js`（命令存在性/内置命令白名单双向校验/快捷键一致/结构合法性/悬停解析，9 断言组）→ 全量 **53 pass**；headless EXIT 0；
> - 文档：对齐对照新增「菜单对齐」章节（逐菜单对照 + 不移植清单 + 快捷键对齐说明）、使用说明/README/本文件同步 0.8.70-dev。
>
> 本轮完成**第四十轮 UI 排布优化**（0.8.62-dev，含 A1/B2/C1 还原 + 空态与图标微调）：
> - 视图排布：**Build Log 移至底部 Panel 容器 `codeblocks-buildPanel`**（更贴近 Code::Blocks 底部日志）；侧栏仅剩 Menu → Project → Symbols；
> - 默认大小：`initialSize` flex 权重 Menu 7 / Project 38 / Symbols 9（约 13% / 70% / 17%，仅新装或重置时生效）；
> - 一次性排布应用：升级后首次激活用 `vscode.moveViews` 重排（globalState `codeblocks.uiLayoutVersion` 标记，失败不写标记并重试 3 次，成功后不覆盖用户手动调整）；视图位置/大小由 VS Code 原生持久化；
> - 新增命令 `codeblocks.resetViewLayout`（重置视图布局）；`codeblocks.buildLog.focus` 改为聚焦底部 Panel 容器；
> - 还原 A1（状态栏诊断徽标）/ B2（文件类型图标）/ C1（Build Log Emoji 根节点）；`viewsWelcome` 仅保留 Project 空态引导；Menu 视图 codicon 图标（9 顶级菜单 + 子项）；
> - **教训**：视图容器 ID 不能含 `.`（schema `^[a-zA-Z0-9_-]+$`）——`codeblocks.buildPanel` 导致容器注册失败、视图掉进 Explorer、`vscode.moveViews` 静默跳过；改名 `codeblocks-buildPanel` 并升级布局版本号强制重应用；`code --install-extension --force` 后必须 Reload Window 新清单才生效。
> 当前版本 **0.8.80-dev**，全量测试 71 pass。
> 未实现功能盘点（第二轮，A–E 共 33 项 + 不移植清单）见 `docs/未实现功能待办.md`（2026-09-27 已更新：第三批 14 项实施完毕，B1/B2/C4 评估后不做）。
>
> **第五十三轮 配置（Settings）整理（0.8.79-dev）**——36 项设置全面盘点（交叉核验：全部被代码读取、无死配置/隐形配置）后实施优化：
> - **设置页 7 分区**（VS Code 内置扩展同款 `configuration` 数组机制，本机安装包核实）：编译器与工具链 / 构建与日志 / 调试 (GDB) / IntelliSense (clangd) / 界面 / 编辑器辅助与工具 / 快捷键；设置 ID/默认值/覆盖层级/代码读取**零变化**；
> - **scope**：masterPath / compilerPrograms / debug.gdbPath → `machine-overridable`（不参与 Settings Sync、仍可按工作区覆盖）；**描述清理**：gdbPath 去掉「（第五十轮；…）」内部标记；
> - **数值校验**：parallelJobs 0–64、gdbTimeoutMs ≥1000、maxReportedErrors ≥0、printElements ≥0；compilerPrograms 补 DBGconfig 属性；
> - **新增 3 设置**：editor.tidyCommentWidth（Tidy 换行宽度，默认 80）、editor.headerGuardStyle（`ifndef`/`pragma-once`）、ui.recentProjectsLimit（最近工程上限，默认 8，0=不记录）；Settings 菜单新增 `Build & Log…`、`clangd / IntelliSense…` 入口；
> - 测试：新增 tests/test-settings-structure.js（22 断言：分区块/总数 39/无重复/默认值/枚举/scope/校验字段/**「全部键在 dist 中被读取」防死配置** + nls 占位符/双语言键完整性）+ 头文件保护/注释整理/设置链接测试补充 → 全量 **71 pass**；
> - **补充（0.8.80-dev）**：分区标题改 nls 占位符——`package.nls.json`（英文默认：Compiler & Toolchain / Build & Log / Debug (GDB) / IntelliSense (clangd) / UI / Editor & Tools / Keybindings）+ `package.nls.zh-cn.json`（中文），**跟随 VS Code 显示语言**。
>
> **第五十二轮 第三批全量实施（0.8.78-dev）**——待办清单剩余可实施项一次做完（14 项），B1/B2/C4 经评估确认**不做**（方案 A）：
> - **Wave 1 编辑器/工具**：D1 头文件保护（命令 + `codeblocks.editor.autoHeaderGuard` 新建空头文件自动插入，幂等）、D3 Tidy 注释（选中块注释：对齐 `*`/空格规范/超宽换行，幂等）、D4 Edit 菜单补项（Encoding/EOL/Highlight Mode/Parameter Hints/Find Next·Previous Selected/Go to Previous·Next Change + **Swap Header / Source** 自实现同名反扩展名查找）、E1 自定义工具（设置 `codeblocks.tools` + Tools 菜单动态条目 + Configure Tools…，终端/输出通道/静默三种输出模式，别名宏 `${file}` 等 + CB `$(VAR)` 展开）。
> - **Wave 2 属性编辑**：C2 外部依赖/附加输出（目标页「依赖与附加输出」分组；writer 补齐 `external_deps`/`additional_output` 写出并从透传 handled 集排除防重复）、C3 项目自定义变量（项目设置「每行 name=value」；Extensions 节点写回，非法变量名跳过告警）；**顺带修复旧 bug**——`<Project><Extensions>` 此前误从根节点读取（自定义变量解析恒空、保存会丢扩展数据），已对照 CB projectloader.cpp:90/238 确认真实位置并修正。
> - **Wave 3**：C1 工作区依赖编辑（Project → Workspace Dependencies…：QuickPick 多选 + 环路检测 + `<Depends>` 条目级文本手术写回 + 依赖映射热重载）、B3 编译器命令查看（Tools → Show Compiler Commands…：各 CommandType 模板 + 活动工程/目标展开预览，虚拟文档）、B4 Makefile 导出（Tools → Export Makefile…：字面命令 + 目录规则 + clean 双格式；**mingw32-make 真机 e2e**——构建/运行/增量/clean 全过）。
> - **Wave 4**：C5 工程导入（File → Import Project…：Dev-C++ `.dev`（INI/XML 两代）、VC6 `.dsp`、VS2010+ `.vcxproj` → 生成 `.cbp` 并可立即打开；工程外文件跳过提示）、C6 新模板（GLFW/SDL2/wxWidgets/Qt，含模板选项与说明文件）。
> - **B1/B2/C4 评估结论（方案 A：不做）**：default.conf 为 CB 本体私有配置（CB 退出/打开设置对话框时整文件覆写，写入竞态不可消）；程序路径已由「Detect Compilers → `compilerPrograms`」+ default.conf `user_sets` 双重覆盖，全局目录/选项/库/变量已被完整读取生效——收益 < 成本。替代：新增 `codeblocks.openDefaultConfig`（Settings → Default Config… 打开 default.conf 手工编辑，保存后扩展读取立即生效）。
> - 测试：新增 9 个测试文件（头文件保护/注释整理/工具运行/外部依赖往返/自定义变量往返/工作区依赖写回/Makefile 导出+真机 make/collectMakefileData/工程导入），全量 **70 pass**；headless 真机构建 SUCCESS。
>
> **文档整理（0.8.76-dev）**：删除 8 份历史存档/过时文档（P0·P1P2·P3 完成报告、解耦提示词、UI 优化提示词、源码测绘报告、编译产物一致性排查经验、优化待办——内容保留于 git 历史）；`docs/release/` 增加目录说明并恢复逐版本发布说明；README 补许可证合规说明；版本号统一 **0.8.76-dev**。
>
> **第五十一轮 调试 P2 三项（0.8.77-dev）**——用户点名可选改进，三项全做：
> - **E1 指令断点**（反汇编视图 gutter 对任意指令下断）：能力声明 `supportsInstructionBreakpoints` + `setInstructionBreakpoints` 处理；地址形式 `-break-insert *ADDR`（GDB 8.1/7.6.1 已实证，与 Step Out 临时断点同路径）；条件/命中次数/日志消息复用文件断点机制（条件为客户端求值）；新纯函数 `parseInstructionReference`（hex/十进制 + 偏移，含单测）。
> - **E2 线程名解析**：`onThreads` 弃用旧惰性可选组正则（与 D3 同款缺陷：name 永不捕获），改用逐 tuple `parseThreadTuples`——实测本机 GDB 实报线程名已可见（缺失时兜底「线程 N」）。
> - **E3 多会话路由**：`debugRegistry` 单指针 → 「会话 id → 适配器」映射 + 最近会话兜底；适配器携带 `session.id`（工厂传入）、注册/注销随会话生命周期；`getActiveAdapter` 聚焦会话优先——多会话时寄存器视图/调试辅助命令跟随聚焦会话，任一会话结束不再误清指针；新增 `onDidChangeActiveDebugSession` 刷新。
> - 测试：e2e（真实 GDB）**14/14**（指令断点全链路、线程名捕获、双会话聚焦优先/兜底/注销不误清）；`tests/test-debug-protocol.js` 增 4 断言（指令地址解析）。
>
> **第四十三轮 工程分析视图（0.8.69-dev）**：
> - **Symbols 默认隐藏**：`visibility: "hidden"`（新装初始隐藏，可从容器 ⋯ → Views 调出）+ 设置 `codeblocks.ui.symbolsView`（默认 true，关闭后所有用户强制隐藏）；
> - **新增 Analysis 视图**（侧栏，默认可见）：按工程展示 `.cbp` 重要属性（概要 / 构建目标 / 构建命令 / 搜索目录 / 文件 / 虚拟目标 / 变量 / 备注）；数据来自已解析模型（零文件读取）；
> - **属性 ↔ .cbp 配置对照**：每行 tooltip = 对应片段（如 `<Option output="…" />`）；点击 = 在 `.cbp` 中定位并高亮（候选文本递退：`属性="值"` → `属性=` → 值）；右键菜单「在 .cbp 中定位」/「复制 .cbp 片段」；
> - **彩色图标**（`resources/analysis-icons/`，11 个）；**默认折叠策略**：最近构建置顶，仅第一个工程的「概要」展开；
> - 新命令：`codeblocks.analysis.refresh` / `codeblocks.analysis.locate` / `codeblocks.analysis.copyXml`（`src/ui/analysisTreeProvider.ts`）。
>
> **第四十二轮 编译器探测优化（0.8.68-dev）**：
> - **P1 立即弹窗**：`detectCompilers` 改用 `createQuickPick()` 实例——点击**立即显示**（busy 进度条 + 缓存预填可立即选择），后台探测完成后自动刷新列表；已选择则直接应用；
> - **P2 异步并行探测**：`detector.ts` 新增 `detectAllCompilersAsync()`（GCC/Clang/MSVC/RISC-V 四组并行；`spawnSync` → `execFile` 异步；RISC-V 目录扫描改 `fs.promises` + `withFileTypes` 免逐项 stat；多工具链版本查询并行）；同步版 API 零改动；
> - **P3 跨会话缓存**：结果存 `globalState`（TTL 24h / masterPath 变化失效）+ 激活 4s 后后台预热 → 新会话首点即瞬时展示；
> - **实测**：同步 1518ms → 异步并行 251ms（约 6×），结果 4 条完全一致（gcc + 3 个 RISC-V）。
>
> **第四十轮补充（0.8.63-dev）**：状态栏新增 **Build Workspace / Rebuild Workspace**（`$(multiple-windows)` 图标，绑定 `codeblocks.buildWorkspace` / `codeblocks.rebuildWorkspace`）；后于 0.8.67-dev 整合进构建菜单（见下），对应设置 `codeblocks.ui.workspaceStatusBar` 已移除。
>
> **第四十一轮 UI 再优化（0.8.67-dev）**：
> - **Menu 状态栏化**：移除侧栏 Menu 视图（`src/ui/menuTreeProvider.ts` 删除）；状态栏最左 `$(menu) Menu`（priority 1000）——点击弹出**两级 QuickPick**（Workspace / Recent Projects / 9 个菜单 → 命令），**悬停就地展开**常用命令链接（trusted Markdown 命令链接）；侧栏变为 Project + Symbols；
> - **构建四项整合**：Build / Rebuild / Build Workspace / Rebuild Workspace 合并为单个 Build 状态栏项——空闲点击弹菜单（Build 首项），构建中变 `$(sync~spin) Building… (Ns)`（实时秒数）且**再次点击停止构建**，悬停提供四项链接 / 停止链接；移除设置 `codeblocks.ui.workspaceStatusBar`；
> - **Project 视图**：标题栏按钮改为**可配置**——新设置 `codeblocks.ui.projectToolbar`（默认 `buildWorkspace/rebuildWorkspace/cleanWorkspace` 三项常驻，13 个候选值，未选中的自动进 ⋯ 溢出菜单，`codeblocks.tb.*` 上下文键驱动，配置即时生效）；项目行新增行内按钮 Build / Rebuild / Clean / **Properties**（新增命令 `codeblocks.cleanProject`，语义同 `clean()` 但作用于点击的工程）；
> - 名称统一：Build Workspace / Rebuild Workspace（不再使用 Build WS / Rebuild WS 简写）。

---

**下一步**：仍不实现的限制——跨卷对象路径、makefile 自定义项目、Squirrel 脚本引擎、console runner、
include_prj_cwd/include_file_cwd、INCDIR 风格 include 开关（详见对齐对照）。
